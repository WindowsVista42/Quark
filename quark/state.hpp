#pragma once

#include <system.hpp>
#include <ext.hpp>

namespace quark::state {
  namespace {
  };

  // Save the current system list under the input name.
  //
  // This can then later be used to "switch states",
  // such as switching between a main menu and
  // the game.
  static void save(const char* name) {
  }
};
